rules:
  unpinned-uses:
    ignore:
      - dependabot-auto-merge.yml
      - docker-build.yml
      - docker-deploy.yml
      - lint.yml
  template-injection:
    ignore:
      - docker-build.yml
  dependabot-cooldown:
    ignore:
      - dependabot.yml
